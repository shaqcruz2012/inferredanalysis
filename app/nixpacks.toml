[phases.setup]
nixPkgs = ["python312"]
providers = ["python"]

[phases.install]
cmds = ["pip install -r /opt/app/requirements.txt || pip install -r requirements.txt"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port 8000"

[variables]
PORT = "8000"
